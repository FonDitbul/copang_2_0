import AccountLogin from "@/components/Account/login";

export default function Account() {
  // TODO 로그인 정보가 없을시 로그인 페이지
  // TODO 로그인 정보가 존재할 시 account 페이지로 이동
  return (
    <div>
      <AccountLogin />
    </div>
  );
}